# TASK: Web 内存与浏览器稳定性修复（更新：2026-07-17 06:49 EDT）

## Why（一句话）

让 `001.100app.dev` 在 5,000+ 商品及“全部地区”条件下保持恒定页面规模，避免高分辨率图片和全量 DOM 导致用户浏览器卡死或被系统强退。

## 当前状态：生产已部署并完成核心验收

## 已确认事实

- 线上默认地区实测为 655 张商品卡、624 张图片、15,903 个 DOM 节点；来源：2026-07-17 浏览器只读运行时检查。
- 线上 `?region=all` 实测为 5,307 张商品卡、5,276 张图片、130,750 个 DOM 节点，首屏约 31.8 秒；来源：2026-07-17 浏览器只读运行时检查。
- “全部地区”页滚动 6,000px 后已加载图片从 24 增至 72，状态检查一度超过 30 秒；来源：2026-07-17 浏览器压力检查。
- 抽样首屏图片多数为 1350x1710，实际显示约 293x366；来源：2026-07-17 浏览器 naturalWidth/clientWidth 检查。
- 线上首页从 Supabase `products` 分页执行 `select('*')`，并将所有过滤结果通过一次 `innerHTML` 渲染；来源：2026-07-17 `curl https://001.100app.dev/`，线上 HTML 1448-1571。
- 线上详情页无条件加载 `data.js`；线上文件 `Content-Length: 9225684`；来源：2026-07-17 `curl -I` 与线上 HTML 545。
- `origin/main` 的 `index.html`、`product-detail.html`、`data.js` SHA-256 与线上完全一致；本地两个 HTML 仅额外包含既有生命周期过滤修改；来源：2026-07-17 `git fetch origin`、`git show ... | shasum`、`git diff origin/main -- ...`。

## 剩余假设与未验证项

- 当前浏览器控制通道无法取得 JS heap 指标；本地稳定性结论基于 DOM、图片数量、图片自然尺寸、页面结构、控制台错误和 10 次分页往返。
- 未取得报告用户设备的崩溃日志，不能把原始事件定性为传统 retained-object 内存泄漏；已确认原页面资源压力足以导致卡死或强退。
- 线上多次点击压力测试的自动化控制通道在滚动到页尾点击时超时；发布前同一代码已完成 10 次分页往返，线上首屏规模与发布候选完全一致。

## 验收标准

- `?region=all` 仍显示完整结果总数，但首屏商品卡不超过 60、图片不超过 60、DOM 节点不超过 2,500。
- 连续翻页和返回至少 10 次后，商品卡、图片、DOM 节点不随操作次数累积。
- 列表缩略图请求使用接近显示尺寸的 Imgix 参数，不再请求 1350x1710 作为 293x366 卡片图。
- 首页不再写入/读取 `products_cache_v1`，不再执行列表 `select('*')`。
- 正常详情页不加载 `data.js`；Supabase 正常时仍显示商品、图片、价格与历史数据。
- 静态测试、HTML 内嵌 JavaScript 语法检查和本地浏览器行为检查通过。

## 已完成且已验证

- 已完成线上/本地/origin 三方来源对齐，确认可在保留现有生命周期过滤的前提下修改。
- 首页改为每页最多 60 张商品卡；5,306 条本地实时结果显示为 89 页，首屏 60 张卡、60 张图片、1,669 个 DOM 节点、约 106KB 网格 HTML。
- 连续向后 5 页再返回 5 页后，每页仍为 60 张卡、60 张图片，DOM 节点保持在 1,648-1,732，未随操作次数累积。
- 列表 Imgix 图片实际请求与自然尺寸为 480x600，不再用 1350x1710 原图作为卡片缩略图。
- 首页正常 Supabase 路径未加载 `data.js`、未出现控制台 warning/error；暖缓存重载到 60 张卡完成耗时 6,616ms。
- 首页移除完整 `products_cache_v1` 读写，并将 Supabase 列表查询改为显式字段集合。
- 详情页正常 Supabase 路径未加载 9MB `data.js`；商品、价格、图片和历史价格正常显示，DOM 158 个、图片 8 张、控制台无 warning/error。
- 详情缩略图实际请求与自然尺寸为 160x200；静态 `data.js` 仅在 Supabase 查询失败时按需加载。
- `python3 -m unittest discover -s tests -v` 在临时虚拟环境中运行 14 项测试，结果 `OK`。
- 两份 HTML 的可执行内嵌 JavaScript 均通过 Node 语法检查；`git diff --check` 无输出。
- 基于最新 `origin/main` 的独立干净工作树运行完整测试套件：43 项测试，结果 `OK`；生产 Supabase 质量门为 `rows=5306 active=5306`、`[quality] OK`。
- 发布提交为 `2c190a6 fix: bound web catalog memory usage`，已直接推送到 `origin/main`。
- Vercel Production 部署 `dpl_71d6Q3zxcTXTtvNiNVD11KvGetDd` 状态为 `Ready`，别名包含 `https://001.100app.dev`。
- 线上 `index.html` SHA-256 `c949d4c81e8eb02107dfdeb7481bdfc1611bc35f04a8b66e2fb214db9a23377b`、`product-detail.html` SHA-256 `59b6e1b5bd5b03149158addd14478ab5a3da911e27af3d8cd758ea9a79793704`，均与发布提交完全一致。
- 线上 `?region=all` 首屏实测为 5,306 条结果、60 张卡、60 张图片、1,669 个 DOM 节点、图片自然尺寸 480x600，且正常路径未加载 `data.js`、控制台无 warning/error。

## 下一步（按序）

1. 观察真实用户是否仍报告卡死或强退，并在能取得设备崩溃日志时补做 retained-object/heap 定性。
2. 若真实用户的首屏等待仍偏长，再把 5,000+ 行筛选数据改为服务端查询或渐进加载；当前直连 Supabase 六页请求合计约 9.5 秒。

## 死路

- CDP `Runtime.getHeapUsage` / `Performance.getMetrics` 在当前浏览器控制通道超时，因此验收采用 DOM、图片数量、自然尺寸、响应时间和重复操作的可证伪指标。

## 边界

- 不覆盖工作区既有未提交改动。
- 本次用户已明确授权发布；仅发布首页、详情页和对应回归测试，其他本地未提交改动均未纳入提交。
